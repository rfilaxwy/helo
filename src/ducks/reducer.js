const initialState ={
    username:'',
    id:'',
    profilePic:''
}

export default function reducer(state=initialState,action){
    let {payload} = action ;
    switch(action.type){
        // case:;
        // case:;
        // case:;
        default:
            return state;
    }
}